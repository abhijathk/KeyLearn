{
  "private": true,
  "name": "@keylearn/settings-database",
  "version": "0.0.0",
  "type": "module",
  "main": "lib/index.ts",
  "types": ".types/index.d.ts",
  "dependencies": {
    "@keylearn/config": "*",
    "@keylearn/settings": "*"
  },
  "devDependencies": {},
  "scripts": {
    "clean": "rm -fr .types",
    "compile": "tsc",
    "test": "tstest --import=@keylearn/test-env-server --test 'lib/**/*.test.{ts,tsx}'"
  }
}
